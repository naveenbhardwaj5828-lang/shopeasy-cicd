# ShopEasy

Simple demo e-commerce backend application for the CI/CD learning lab.

## Endpoints
- `GET /`
- `GET /products`
- `GET /health`

## Run
`python -m pip install -r app/requirements.txt`
`python app/app.py`

## Test
`python -m pip install pytest`
`pytest`
